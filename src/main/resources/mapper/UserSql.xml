<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="data.mapper.UserMapper">
    <insert id="join" parameterType="data.dto.UserDto$Create" useGeneratedKeys="true" keyProperty="id">
        insert into user
            (id, nickname, email, profile_image, credit, subscription_date, college_id, provider_id, provider)
        values
        (null, #{nickname}, #{email}, #{profileImage}, #{credit}, now(), null, #{providerId}, #{provider})
    </insert>

    <select id="findUser" parameterType="data.dto.UserDto$Search" resultType="data.dto.UserDto$User">
        select
            id,
            provider,
            provider_id,
            nickname,
            email,
            profile_image,
            credit,
            subscription_date,
            college_id,
            refresh_token
        from user
        where true
            <if test="id != null">
                AND id = #{id}
            </if>
            <if test="email != null">
                AND email = #{email}
            </if>
    </select>

    <update id="updateUser" parameterType="data.dto.UserDto$Update">
        update user
        <set>
            <if test="nickname != null">
                nickname = #{nickname},
            </if>
            <if test="profileImageUrl != null">
                profile_image = #{profileImageUrl},
            </if>
            <if test="collegeId != null">
                college_id = #{collegeId},
            </if>
            <if test="refreshToken != null">
                refresh_token = #{refreshToken},
            </if>
        </set>
        where id = #{id}
    </update>

    <delete id="deleteUser" parameterType="int">
        delete from user
        where id = #{id}
    </delete>
</mapper>
